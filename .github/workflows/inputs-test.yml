name: Inputs Test

on:
  workflow_dispatch:
    inputs:
      environment:
        type: environment

      probers_enabled:
        description: 'Runs probers when checked'
        default: true
        required: true
        type: boolean


jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Display github.event.inputs.probers_enabled
        run: "echo ${{ github.event.inputs.probers_enabled }}"

      - name: Display github.event.inputs.environment
        run: "echo ${{ github.event.inputs.environment }}"

      - name: Display env.DEPLOY_SUBDOMAIN
        run: "echo ${{ env.DEPLOY_SUBDOMAIN }}"

      - name: Display env.DEPLOY_TARGET
        run: "echo ${{ env.DEPLOY_TARGET }}"
