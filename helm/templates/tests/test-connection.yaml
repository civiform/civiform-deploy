apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "civiform.fullname" . }}-test-connection"
  labels:
    {{- include "civiform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "civiform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
