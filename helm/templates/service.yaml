apiVersion: v1
kind: Service
metadata:
  name: {{ include "civiform.fullname" . }}
  labels:
    {{- include "civiform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "civiform.selectorLabels" . | nindent 4 }}
