#! /usr/bin/env bash

# DOC: Deploy an image to an environment. Usage: bin/deploy --tag=<image tag>

source bin/lib.sh
source civiform_config.sh

CMD_NAME=deploy checkout::exec_delegated_command "$@"
