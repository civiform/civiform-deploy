#! /usr/bin/env bash

# DOC: Deploy an image. Usage: bin/deploy --tag=<image tag> --config=<config_file>

source bin/lib.sh
source ${CONFIG_FILE}

CMD_NAME=deploy checkout::exec_delegated_command "$@"
